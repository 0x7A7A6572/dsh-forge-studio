/**
 * 便签板「使用说明」内容（只读 markdown，弹窗内用 tiptap 只读实例渲染）。
 * 覆盖四块：入口与快捷键 / 基础使用 / 对话使用 / 任务泳道。
 *
 * 文案里的产品事实必须与实现一致（tests/help-content.test.ts 守卫）：
 * - agent 工具名与 note:// 引用语法对齐 src/agent/tools.ts、reference.ts；
 * - 泳道五列状态-纸色映射对齐 src/client/core/task-lanes.ts 的 TASK_LANES；
 * - 五色板与编辑器交互对齐 src/client/core/note-colors.ts。
 * 注意：勿在「任务泳道」分节之外使用整词 待规划/待办/进行中/已完成/已失败
 * （覆盖测试按首次出现顺序断言映射行顺序）。
 */

export const HELP_MARKDOWN = `# 智能便签 · 使用说明

便签板是 DeepSeek Harness 里的一个轻量「随手记 + 轻看板」：以 Markdown 记内容、
按颜色分类、置顶/归档管理，还能把便签当任务卡拖进五列任务泳道。你手写的内容，
会话里的 AI 助手也能读能写——记笔记、查资料、整理任务都可以直接在对话里交代。

## 一、入口与快捷键（在 DeepSeek Harness 里怎么用）

- 左侧栏点 **智能便签**（新建会话按钮下方）即可打开/收起中间列的便签板。
- 便签板右上角：**说明**（本弹窗）、**设置**（改「默认标题」）、**刷新**（立即重新拉取）、**关闭**；左上角「返回」回到会话。
- 便签随写随存，刷新页面不丢；打开便签板会自动加载，之后每约 5 秒静默同步一次。
- 键盘：**Esc** 逐层关闭（说明 → 设置 → 编辑器 → 整个便签板）；编辑器中 **Ctrl+Enter**（macOS 为 Cmd+Enter）保存、**Esc** 关闭弹窗，**Ctrl+K**（Cmd+K）直接打开「插入链接」。

## 二、基础使用

### 新建与编辑
- 工具栏点 **新建便签**，在弹窗里写标题与正文。
- 正文是 **Markdown**：标题、列表、引用、代码块、分隔线、**链接**、**表格**都能用；工具栏提供一键格式（加粗/斜体/删除线/标题 1-3/无序与有序列表/引用/行内代码/代码块/**链接**/**表格**/分隔线/撤销重做）。
- **链接**：选中文字点工具栏链接钮（或按 Ctrl+K）输入地址即可；直接粘贴 URL、输入完 URL 后敲空格也会自动成链；再点链接钮可改地址或移除。
- **表格**：点工具栏表格钮，在网格里拖选行列数插入；光标在表格内时同钮变成行/列增删与整表删除操作。
- **代码块**：光标进入代码块后，工具栏会浮现**语言下拉**——选中语言立即**语法高亮**（HTML/JavaScript/TypeScript/React(JSX/TSX)/Vue/CSS/Shell/PowerShell/BAT/Python/JSON/YAML/SQL/XML/Markdown/Diff/INI 等）；语言随 Markdown 围栏一并保存。
- 图片可直接 **粘贴** 进正文（自动内联保存）。
- 标题留空自动用「默认标题」，可在右上角设置里修改。
- 示例正文：

\`\`\`markdown
## 周例会记录
- 下阶段排期已确认（见排期表）
- 遗留问题：\`bug-1024\` 联调中
> 结论：周五前出修复版本
\`\`\`

### 管理与查找
- 三种视图一键切换：**纸卡墙**（默认）/ **任务泳道** / **行式列表**；视图与筛选在开关浮层之间不丢失。
- 顶部搜索框同时搜**标题与正文**；下方色点可**按颜色筛选**。
- 每张卡片可 **编辑 / 置顶 / 归档 / 删除**：置顶排最前，其余按最近更新排序。
- 归档便签收进底部折叠区（不计数、不打扰）；在行式列表里展开可恢复或彻底删除。
- 颜色是便签自带的分类维度：编辑器底部的纸色点即改色；在「任务泳道」里它同时代表任务状态（见下）。

## 三、对话使用：让 AI 助手帮你记、查、整理

装好本插件后，会话里的 AI 自动获得便签工具（\`notes_*\`），可以用自然语言让它干活：

- “把这段会议结论记成一张便签：……”（AI 会调 \`notes_create\`）
- “列出我所有便签”（\`notes_list\`：id、标题、颜色、置顶/归档标记）
- “查一下标题为「X」的便签全文”（\`notes_get\`：按 id 读正文）
- “把标题为「X」的便签正文改成……”（\`notes_update\`）
- “把「X」置顶 / 归档”（\`notes_set_pinned\` 或 \`notes_update\`）
- “删掉你刚才建的那张便签”（\`notes_delete\`，只能删 AI 自己建的）

要点与边界：

- **同一份数据**：AI 与便签板读写同一批便签——AI 新建的会立刻出现在板上，你在板上写的 AI 也能查到。
- **权限**：AI 可自由**读取与更新**任意便签；但**不能删除** \`user\` 便签（你在便签板手写的）——这是安全策略的硬性拒绝，目的是保护你的手写内容。AI 自己创建的便签记 \`agent\`，可随意删改。
- **写操作会先征求同意**：AI 新建/修改/置顶/删除前通常先弹出确认，批准后才执行。
- **引用便签**：对话里出现 \`@[标题](note://<便签id>)\` 这类引用时，AI 会先调 \`notes_get\` 读全文再回答；id 可用 \`notes_list\` 查到。

## 四、任务泳道：把便签变成轻量看板

在「任务泳道」视图里，便签就是任务卡——**纸色即任务状态**，五列与五色一一对应：

- 待规划 = 灰
- 待办 = 黄
- 进行中 = 蓝
- 已完成 = 绿
- 已失败 = 粉

用法：

- 把卡片**拖到目标列**即完成换列（便签同时改成该列颜色）；在编辑器底部点颜色也能让它在泳道里换列——两种操作读写的是同一件事。
- 泳道列内自带滚动、便于快速扫视；卡片照常可编辑、置顶、归档。
- **归档便签不进泳道**（归档 = 离开工作流）；泳道底部有提示，去行式列表可管理归档内容。
- 想看某张卡细节或改标题/正文，点卡片即可编辑，保存后回到对应列。
`
