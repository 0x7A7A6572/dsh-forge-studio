/**
 * 便签板「使用说明」内容（只读 markdown，弹窗内用 tiptap 只读实例渲染）。
 * 覆盖四块：入口与快捷键 / 基础使用 / 对话使用 / 任务泳道。
 *
 * 文案里的产品事实必须与实现一致（tests/help-content.test.ts 守卫）：
 * - agent 工具名与工具实现对齐 src/agent/tools.ts；
 * - 泳道五列按任务状态（note.lane.status）分列对齐 src/client/core/task-lanes.ts
 *   的 TASK_LANES；六色仅为纸色、与任务状态无关，对齐 src/client/core/note-colors.ts；
 * - 执行/重置/结果区交互对齐 src/client/components/TaskLaneCard.tsx、NoteEditor.tsx
 *   （点「执行」= 一次性授权；手动改状态/取消任务 = 接管）。
 * 注意：勿在「任务泳道」分节之外使用整词 待规划/待办/进行中/已完成/已失败
 * （覆盖测试按首次出现顺序断言映射行顺序）。
 */

export const HELP_MARKDOWN = `# 智能便签 · 使用说明

便签板是 DeepSeek Harness 里的一个轻量「随手记 + 轻看板」：以 Markdown 记内容、
按六色纸卡分类、置顶/归档管理，还能把便签标记成任务、放进按状态分列的任务泳道，
甚至点一下「执行」让会话里的 AI 帮你把任务跑完并写回结果。你手写的内容，会话里的
AI 助手也能读能写——记笔记、查资料、整理任务都可以直接在对话里交代。

## 一、入口与快捷键（在 DeepSeek Harness 里怎么用）

- 左侧栏点 **智能便签**（新建会话按钮下方）即可打开/收起中间列的便签板。
- 便签板右上角：**说明**（本弹窗）、**刷新**（立即重新拉取）、**关闭**；左上角「返回」回到会话。
- 便签的设置（默认标题、默认工作区、入口开关、WebDAV 备份）在 **dsh 设置 → 便签** 里，板内不再有设置入口。
- 便签随写随存，刷新页面不丢；打开便签板会自动加载，此后任何写操作或外部变更都会即时推送刷新（事件驱动，无定时轮询）。
- 键盘：**Esc** 逐层关闭（说明 → 编辑器 → 整个便签板）；编辑器中 **Ctrl+Enter**（macOS 为 Cmd+Enter）保存并关闭、**Esc** 关闭弹窗，**Ctrl+K**（Cmd+K）直接打开「插入链接」，**Ctrl+S**（Cmd+S）保存但**不关闭**弹窗。
- 编辑既有便签时**自动保存**：改动停顿约 1 秒即落盘（页脚有「自动保存 / 已自动保存」指示灯），Ctrl+S 可立刻保存——两者都不退出弹窗，边改边看很方便。新建便签只走显式保存（保存即创建）。

## 二、基础使用

### 新建与编辑
- 工具栏点 **新建便签**，在弹窗里写标题与正文。
- 正文是 **Markdown**：标题、列表、**任务清单**、引用、代码块、分隔线、**链接**、**表格**都能用；工具栏提供一键格式（加粗/斜体/删除线/标题 1-3/无序与有序列表/**任务清单**/引用/行内代码/代码块/**链接**/**表格**/分隔线/撤销重做）。
- **链接**：选中文字点工具栏链接钮（或按 Ctrl+K）输入地址即可；直接粘贴 URL、输入完 URL 后敲空格也会自动成链；再点链接钮可改地址或移除。
- **表格**：点工具栏表格钮，在网格里拖选行列数插入；光标在表格内时同钮变成行/列增删与整表删除操作。
- **任务清单**（todolist）：工具栏点 **任务清单**（快捷键 **Ctrl+Shift+9**）插入勾选项，输入 \`- [ ] \` 也会自动转换；勾上即写回 \`- [x]\`、取消勾选回到 \`- [ ]\`，回车续行、Tab 缩进成子项都有。纸卡与行列表会在标题旁显示完成度（如 2/5），一眼看出还剩几件。
- **代码块**：选中多行再点工具栏代码块钮，会合并成**一个**代码块（不会再一行一块）；光标进入代码块后，工具栏会浮现**语言下拉**——选中语言立即**语法高亮**（HTML/JavaScript/TypeScript/React(JSX/TSX)/Vue/CSS/Shell/PowerShell/BAT/Python/JSON/YAML/SQL/XML/Markdown/Diff/INI 等）；语言随 Markdown 围栏一并保存。
- 图片可直接 **粘贴** 进正文（自动内联保存）。
- 标题留空自动用「默认标题」，可在 **dsh 设置 → 便签** 里修改。
- 示例正文：

\`\`\`markdown
## 周例会记录
- 下阶段排期已确认（见排期表）
- 遗留问题：\`bug-1024\` 联调中
> 结论：周五前出修复版本
\`\`\`

### 管理与查找
- 三种视图一键切换：**纸卡墙**（默认）/ **任务泳道** / **行式列表**；视图与筛选在开关浮层之间不丢失。
- 顶部搜索框同时搜**标题与正文**；下方色点可**按颜色筛选**。
- 每张卡片可 **编辑 / 置顶 / 归档 / 删除**：置顶排最前，其余按最近更新排序。
- 归档便签收进底部折叠区（不计数、不打扰）；在行式列表里展开可恢复或彻底删除。
- 颜色是便签自带的分类维度（六色纸卡）：编辑器底部的纸色点即改色；颜色与任务状态无关，任务状态在任务泳道里按列显示（见下）。

## 三、对话使用：让 AI 助手帮你记、查、整理

装好本插件后，会话里的 AI 自动获得便签工具（\`notes_*\`），可以用自然语言让它干活：

- “把这段会议结论记成一张便签：……”（AI 会调 \`notes_create\`）
- “列出我所有便签”（\`notes_list\`：id、标题、颜色、置顶/归档标记）
- “查一下标题为「X」的便签全文”（\`notes_get\`：按 id 读正文）
- “把标题为「X」的便签正文改成……”（\`notes_update\`）
- “把「X」置顶 / 归档”（\`notes_set_pinned\` 或 \`notes_update\`）
- “删掉你刚才建的那张便签”（\`notes_delete\`，只能删 AI 自己建的）
- “推进标题为「X」的任务状态 / 结束并写结果”（\`notes_task_set_status\` / \`notes_task_report\`，仅泳道「执行」授权后可用，见「任务泳道」）

要点与边界：

- **同一份数据**：AI 与便签板读写同一批便签——AI 新建的会立刻出现在板上，你在板上写的 AI 也能查到。
- **权限**：AI 可自由**读取与更新**任意便签；但**不能删除** \`user\` 便签（你在便签板手写的）——这是安全策略的硬性拒绝，目的是保护你的手写内容。AI 自己创建的便签记 \`agent\`，可随意删改。
- **写操作会先征求同意**：AI 新建/修改/置顶/删除前通常先弹出确认，批准后才执行。

## 四、任务泳道：把便签变成可让 AI 执行的任务看板

任务泳道按**任务状态**分五列：待规划 → 待办 → 进行中 → 已完成 → 已失败。只有**显式标记为任务**的便签才进泳道，普通便签不进任何列；六种纸色（黄/蓝/绿/粉/紫/灰）只是卡片底色、自由分类，**与任务状态无关**。

### 把便签变成任务
- 编辑器里打开「设为任务」开关并选初始状态（默认「待办」）保存，即成为任务；关掉开关保存即「取消任务」（回到普通便签，正文不删）。
- 泳道列头点「＋」可直接在该列新建一条任务。
- 任务卡照常可编辑、置顶、归档；**归档便签不进泳道**。

### 指定工作区（任务在哪里干活）
- 任务是「在某个目录里跑的事」：编辑器任务区有 **工作区** 下拉，选项来自**最近会话用过的目录**（只选不手填；选项只显示文件夹名，悬停看完整路径）。
- 留空 = 用**默认工作区**：设置里的「默认工作区」优先，没配则自动取最近使用的目录，再没有才用宿主进程目录——任务总归有一个明确的工作区。
- 任务卡上有显式工作区时会显示目录名（悬停看完整路径）；未显示即表示用默认工作区。

### 换状态
- 把任务卡**拖到目标列**即改状态；在编辑器里改状态下拉同样生效——两者读写同一状态。
- 泳道列内自带滚动、便于扫视；搜索仍可用（色筛在泳道里隐藏，因为列已按状态分列）。

### 点「执行」让 AI 帮你做
- 非「进行中」的任务卡 hover 有「执行」按钮（已完成/已失败显示「重跑」）：点一次即**一次性授权** AI 执行该任务——读便签全文、把状态推进到「进行中」、按描述干活，最后把结果写回。
- 执行会**按任务的工作区新建一个会话**（工作目录 = 工作区），任务在那个新会话里跑：与你和 AI 正在聊的会话互不打扰，也不会把工作目录搞混；新会话会出现在会话列表里，可以随时点进去看进度、追问或接管。
- 任务便签**没单独指定工作区**时用「默认工作区」（设置值 → 最近用过的目录 → 宿主进程目录），执行照常；这种情况下新会话在会话列表里可能落在「（未分组）」，而**指定了工作区**（且该目录已是会话列表里的工作区）时会归进对应目录分组。
- 执行中（进行中）卡片显示转圈与**已耗时**，hover 有「重置为待办」；耗时超过 30 分钟还会弱提示「执行可能已中断，可重置」。觉得跑偏或已中断，点「重置为待办」即可收回控制。
- 执行协议只让 AI 操作该任务的状态与结果（\`notes_task_set_status\` / \`notes_task_report\` 两个窄权限工具），不改正文、标题、颜色。
- 结果写进任务区：已完成/已失败卡显示结果摘要首行；点卡片进编辑器，在只读「任务与结果」区看完整摘要（AI 写的 markdown 总结）。

### 定时执行（到点自动派发）
- 编辑器任务区有 **定时** 开关：打开后选周期——**间隔**（每 N 分钟/小时）、**每天**、**每周**（可多选星期几）、**每月**（几号，当月不足时落在当月最后一天），以及 **一次性**（指定某个日期时刻）。
- 到点由宿主自动派发一次，等价于你点「执行」（同样按工作区新建会话），**不需要便签板开着**；摘要行只显示「下次时刻 · 还有多久」，卡片上挂同一行；上次结果、共跑次数与这句后果说明收进「详情」，点开才看。
- **一次性**：触发成功后自动停用（便签上留「上次：已派发」的记录）；派发失败会写明原因并停用，不会反复重试。
- **循环**：跑完**回到「待办」**等下一个周期（执行结果照旧写在卡片摘要与编辑器里）；上一次还在跑时本轮跳过，下一个周期再试。
- **状态闸门**：卡片在 **待规划 / 已完成 / 已失败** 时不会被自动执行——待规划还没定下来要做，已完成/已失败说明这一轮结束了，系统不擅自再跑起来。这是**软闸门**：日程配置与时间轴都保留，把卡片拖回「待办」即自动恢复（无需重开开关）。
- **错误边界**：连续失败 3 次（派发失败或运行失败）会自动停用日程并写明原因，不会无限重试；定时派发出去的 run 超过 30 分钟仍未收尾，由宿主按失败收尾（避免便签永久卡在「进行中」）。卡片悬停可看「共跑 N 次 / 连续失败 N 次」，失败次数直接以红字挂在卡片上。
- 便签被**归档**或**取消任务**时不触发（硬闸门）；取消任务会一并清掉定时。宿主没运行时错过的时点不补跑，重启后按「下一次」重新排。
- 想彻底停掉就把「定时」开关关掉再保存。
### 手动接管
- 你**任何手动改状态**（拖列 / 编辑器改状态 / 「重置为待办」/「取消任务」）都会立即**收回 AI 的执行授权**；之后 AI 再想动这个任务会被拒绝（提示任务已被手动接管）。
`